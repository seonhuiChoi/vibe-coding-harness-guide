# 줄바꿈을 LF로 고정한다.
# 윈도우 기본값(core.autocrlf=true)으로 체크아웃하면 harness/*.sh 가 CRLF가 되고
# "#!/usr/bin/env bash\r" 때문에 셸 스크립트가 전부 깨진다.
* text=auto eol=lf

*.sh   text eol=lf
*.html text eol=lf
*.md   text eol=lf
*.json text eol=lf
